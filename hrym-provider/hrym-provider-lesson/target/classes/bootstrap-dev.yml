spring:
  cloud:
    config:
      profile: dev
      label: master
      name: datasource,redis,eureka,zipkin,txlcn
      uri: http://127.0.0.1:5588/
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1
